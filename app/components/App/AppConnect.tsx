import { Button, Card } from '@mantine/core'

export default function AppConnect() {
  return (
    <Card>
      <Button onClick={() => { }} className="button-connect">
        Does nothing
      </Button>
    </Card>
  )
}
